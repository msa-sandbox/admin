version: "3.8"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "80:5173"

#  app-prod:
#    build:
#      context: .
#      dockerfile: Dockerfile
#      target: nginx
#    ports:
#      - "8080:80"
